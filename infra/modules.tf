# Observação: quando você executa Terraform dentro de infra/inventories/dev,
# quem referencia o módulo é o próprio `inventories/dev/main.tf`.
# Este arquivo existe apenas para manter o padrão de estrutura.
